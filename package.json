{
  "name": "electron-auto-update",
  "version": "1.0.1",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build  --win --publish never",
    "deploy": "electron-builder build  --win --publish always"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^15.5.5",
    "electron-builder": "^22.4.1"
  },
  "build":{"artifactName": "setup-${version}.${ext}"},
  "repository": {
    "type" : "git",
    "url" : "https://github.com/shyamjptpm/electron-auto-update.git"
  },
  "dependencies": {
    "electron-updater": "^4.2.5"
  }
}
